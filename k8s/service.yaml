apiVersion: v1
kind: Service
metadata:
  name: hello-svc
  namespace: demo
spec:
  type: NodePort
  selector:
    app: hello
  ports:
    - port: 80
      targetPort: 80
      # nodePort: 3xxxx   # ha fix NodePort kell, itt megadhatod (30000–32767)
